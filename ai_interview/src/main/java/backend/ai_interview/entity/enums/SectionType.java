package backend.ai_interview.entity.enums;

/**
 * SectionType Enum
 *
 * Defines the supported resume section types
 * used in ResumeSection entity.
 *
 * These correspond to editable sections in the resume editor.
 */
@SuppressWarnings("all")
public enum SectionType {

    SUMMARY,
    SKILLS,
    EXPERIENCE,
    PROJECTS,
    EDUCATION,
    CERTIFICATIONS,
    ACHIEVEMENTS,
    PUBLICATIONS,
    VOLUNTEERING,
    LANGUAGES,
    INTERESTS,
    REFERENCES,
    GENERAL
}